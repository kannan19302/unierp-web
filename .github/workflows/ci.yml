name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  app-gate:
    uses: kannan19302/unierp-workspace/.github/workflows/application-gate.yml@main
  policy-gate:
    uses: kannan19302/unierp-workspace/.github/workflows/policy-gate.yml@main
    with:
      repo: unierp-web
